import { ContainRotatedElementDirective } from './contain-rotated-element.directive';

describe('ContainRotatedElementDirective', () => {
  it('should create an instance', () => {
    const directive = new ContainRotatedElementDirective();
    expect(directive).toBeTruthy();
  });
});
